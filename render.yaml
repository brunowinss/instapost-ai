services:
  - type: web
    name: instascheduler-ai
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false # Set this in Render dashboard (PostgreSQL URL)
      - key: PORT
        value: 3000
